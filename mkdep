#!/usr/bin/env rc
echo 'MKSHELL=rc'
exec awk '
	/^[^ \t].*:/ {
		sub(/:.*:/, ":VQ:")
		rule = 1
		print
	}
	/^[ \t]/ && rule {
		print "  echo $target"
		print ""
		rule = 0
		next
	}
' $ports/*/port.mk 
